<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet permission to allow API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceApp">

        <activity
            android:name=".activities.BrowseProductsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.OrderTrackingActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ProductDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.ManageCartActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".activities.UpdateProductActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".activities.AddProductActivity" -->
        <!-- android:exported="false" /> &lt;!&ndash; MainActivity: Entry point for the app &ndash;&gt; -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Cart Activity -->
        <activity
            android:name=".activities.CartActivity"
            android:exported="false" /> <!-- Checkout Activity -->
        <activity
            android:name=".activities.CheckoutActivity"
            android:exported="false" /> <!-- Search Activity -->
        <activity
            android:name=".activities.SearchActivity"
            android:exported="false" /> <!-- Product Activity -->
        <activity
            android:name=".activities.ProductActivity"
            android:exported="false" /> <!-- Order Activity -->
        <activity
            android:name=".activities.OrderActivity"
            android:exported="false" />
    </application>

</manifest>